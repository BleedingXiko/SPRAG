from sprag import Controller


class WatcherDemoController(Controller):
    route = "/watcher-demo"

    def load(self):
        pulse = self.service("pulse_service")
        return pulse.get_state()

from sprag import mount, shell

from .modules import LifecycleDemoModule
from .server import LifecycleDemoBoot
from .web import LifecycleDemoApp


lifecycle_demo = mount(
    path="/lifecycle-mount",
    component=LifecycleDemoApp,
    module=LifecycleDemoModule,
    boot=LifecycleDemoBoot,
    shell=shell(css=["app/mounts/lifecycle_demo/lifecycle.css"]),
    metadata={"title": "Lifecycle Mount"},
)

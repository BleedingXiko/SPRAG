from sprag import Controller


class {{class_name}}Boot(Controller):
    route = "{{mount_path}}"

    def load(self):
        return {
            "title": "{{class_name}}",
            "view": "home",
            "switches": 0,
            "message": "{{class_name}} is running client-side.",
        }
